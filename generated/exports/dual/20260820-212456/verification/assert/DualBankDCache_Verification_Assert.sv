// Generated by CIRCT firtool-1.139.0

// Users can define 'STOP_COND' to add an extra gate to stop conditions.
`ifndef STOP_COND_
  `ifdef STOP_COND
    `define STOP_COND_ (`STOP_COND)
  `else  // STOP_COND
    `define STOP_COND_ 1
  `endif // STOP_COND
`endif // not def STOP_COND_

// Users can define 'ASSERT_VERBOSE_COND' to add an extra gate to assert error printing.
`ifndef ASSERT_VERBOSE_COND_
  `ifdef ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ (`ASSERT_VERBOSE_COND)
  `else  // ASSERT_VERBOSE_COND
    `define ASSERT_VERBOSE_COND_ 1
  `endif // ASSERT_VERBOSE_COND
`endif // not def ASSERT_VERBOSE_COND_
module DualBankDCache_Verification_Assert();
  `ifndef SYNTHESIS
    always @(posedge DualBankDCache.clock) begin
      automatic logic _GEN;
      _GEN = DualBankDCache.io_cpu_0_request & DualBankDCache.io_cpu_1_request;
      if (~DualBankDCache.reset
          & ~(~DualBankDCache.io_cpu_0_valid | DualBankDCache.io_cpu_0_request)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: DCache lane valid requires a matching request identity\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~DualBankDCache.reset
          & ~(~DualBankDCache.io_cpu_1_valid | DualBankDCache.io_cpu_1_request)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: DCache lane valid requires a matching request identity\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~DualBankDCache.reset
          & ~(~DualBankDCache.banks_0_io_cpu_valid | DualBankDCache.io_cpu_0_request
              & DualBankDCache._bankHasRequest_T | DualBankDCache.io_cpu_1_request
              & DualBankDCache._bankHasRequest_T_2)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: DCache bank valid requires a lane request for that bank\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~DualBankDCache.reset
          & ~(~DualBankDCache.banks_1_io_cpu_valid | DualBankDCache.io_cpu_0_request
              & DualBankDCache.laneBank_0 | DualBankDCache.io_cpu_1_request
              & DualBankDCache.laneBank_1)) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: DCache bank valid requires a lane request for that bank\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~DualBankDCache.reset & _GEN
          & DualBankDCache.laneBank_0 == DualBankDCache.laneBank_1) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: same-bank LSU requests must be serialized before DualBankDCache\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (_GEN & ~DualBankDCache.reset
          & DualBankDCache.io_cpu_0_valid != DualBankDCache.io_cpu_1_valid) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: dual-bank LSU requests must be accepted atomically\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~DualBankDCache.reset & DualBankDCache.io_axi_wr_done
          & DualBankDCache._banks_1_io_axi_wr_rdy_T) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: AXI write completion arrived without a bank owner\n");
        if (`STOP_COND_)
          $fatal;
      end
      if (~DualBankDCache.reset & DualBankDCache.banks_0_io_axi_wr_done
          & DualBankDCache.banks_1_io_axi_wr_done) begin
        if (`ASSERT_VERBOSE_COND_)
          $error("Assertion failed: AXI write completion must target exactly one DCache bank\n");
        if (`STOP_COND_)
          $fatal;
      end
    end // always @(posedge)
  `endif // not def SYNTHESIS
endmodule

